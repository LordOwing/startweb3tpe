From 697b940bced7c83f94c1fee190f2105f29c2e65a Mon Sep 17 00:00:00 2001
From: sinisti <spideralan2008@gmail.com>
Date: Tue, 8 Oct 2024 15:18:21 +0200
Subject: [PATCH 3/7] update

---
 app/functions/helper.function.php | 16 ++++++++++++
 index.php                         | 43 ++++++++++++++-----------------
 templates/users.html.php          | 12 ++++-----
 3 files changed, 40 insertions(+), 31 deletions(-)

diff --git a/app/functions/helper.function.php b/app/functions/helper.function.php
index 168d40f..8955c08 100644
--- a/app/functions/helper.function.php
+++ b/app/functions/helper.function.php
@@ -6,4 +6,20 @@ function showStatusIcon($status){
         return '<i class="fa fa-check text-success" aria-hidden="true"></i>';
     };
 }
+function showMessage($message_type, $message_text){
+    $html = '<div class="container">';
+    switch($message_type){
+        case 'success':
+            $html .= '<div class="alert alert-success" role="alert">';
+            break;
+        case 'warning':
+            $html .= '<div class="alert alert-warning" role="alert">';
+            break;
+}
+$html .= $message_text;
+$html .= '</div></div>';
+if(!empty($message_text) && !empty($message_type)){
+    echo $html;
+}
+}
 ?>
\ No newline at end of file
diff --git a/index.php b/index.php
index 614183f..3be381d 100644
--- a/index.php
+++ b/index.php
@@ -3,39 +3,27 @@ header('Content-Type: text/html; charset=UTF-8');
 require('app/config/config.php');
 require('app/config/db.php');
 require('app/functions/validate.function.php');
-require('app\functions\helper.function.php');	
-if($_SERVER['REQUEST_METHOD'] == 'POST')
-{	
+require('app/functions/helper.function.php');	
+if($_SERVER['REQUEST_METHOD'] == 'POST'){	
 	fieldRequired('Imię', $_POST['name']);
 	fieldRequired('Nazwisko', $_POST['surname']);
 	fieldRequired('E-mail', $_POST['email']);
 	fieldRequired('Hasło', $_POST['password']);
-	if(!$isError)
-	{
+	if(!$isError){
 		isEmail('E-mail', $_POST['email']);
 	}
-
-	if (!$isError)
-	{	
+	if (!$isError){	
 		$password = md5(PASS_SALT . $_POST['password']);
 		$query = "INSERT INTO users SET user_name = '{$_POST['name']}', user_surname = '{$_POST['surname']}', user_email = '{$_POST['email']}', user_password = '$password'";
-		if ($db->query($query))
-		{
-			echo '<div class="alert alert-success" role="alert">
-  				  Data inserted succesfully!
-				  </div>';
+		if ($db->query($query)){
+			showMessage('success', 'Data inserted succesfully!');
 		}
-		else
-		{
-			echo '<div class="alert alert-danger" role="alert">
-  				  Data has not been inserted!
-				  </div>';
+		else{
+			showMessage('warning', 'Data has not been inserted!');
 		}
 	}
 }
-
 ?>
-
 <!DOCTYPE html>
 <html data-bs-theme="dark">
     <head>
@@ -45,16 +33,23 @@ if($_SERVER['REQUEST_METHOD'] == 'POST')
 
 		<script type="text/javascript" src="assets/js/bootstrap/bootstrap.min.js"></script>
 	</head>
-	
 	<body>
 		<main>
 			<section class="content">
 				<?php include ('templates/form.html.php'); ?>
 			</section>
 			<section class="content">
-				
-
-				<?php include ('templates/users.html.php'); ?>
+				<?php
+					$action = $_REQUEST['action'];
+					$id = (int) $_REQUEST['id'];
+					switch ($action)
+					{
+						case 'delete':
+							echo 'delete';
+							break;
+					}
+					include ('templates/users.html.php');	
+				?>
 			</section>
 		</main>
 	</body>
diff --git a/templates/users.html.php b/templates/users.html.php
index d654d61..0df5f98 100644
--- a/templates/users.html.php
+++ b/templates/users.html.php
@@ -2,26 +2,24 @@
 <div class="container">
 <?php
 $html = '<table class="table">';
-$html .= '<tr><th>ID</th><th>imię i nazwisko</th><th>email</th><th class="text-center">aktywność</th></tr>';
+$html .= '<tr><th>ID</th><th>imię i nazwisko</th><th>email</th><th class="text-center">aktywność</th><th class="text-center">akcje</th></tr>';
 $sql = "SELECT * FROM users";
 $res = $db->query($sql);
-if ($res->num_rows > 0)
-{
-	while ($row = $res->fetch_assoc())
-	{
+if ($res->num_rows > 0){
+	while ($row = $res->fetch_assoc()){
         $name = $row['user_name'] . ' ' . $row['user_surname'];
 		$html .= '<tr>';
         $html .= '<td>' . $row['id'] . '</td>';
         $html .= '<td>' . $name . '</td>';
         $html .= '<td>' . $row['user_email'] . '</td>';
         $html .= '<td class="text-center">' . showStatusIcon($row['active']) . '</td>';
+        $html .= '<td class="text-center">' . '<a href="index.php?action=delete&id=' . $row['id'] . '"><i class="fa fa-times text-danger" aria-hidden="true"></i> usuń</a></td>';
         $html .= '</tr>';
 	}
     $html .= '</table>';
     echo $html;
 }
-else
-{
+else{
     echo '<p class="text-danger">Brak rekordów</p>';
 }
 ?>
-- 
2.46.0.windows.1

